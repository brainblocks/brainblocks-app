/* @flow */
import models from '../models';

const { PasswordReset, User } = models.models;

export default class Email {
    static sendMail() {

    }

    static sendPasswordReset(pr : PasswordReset, user : User) {
        
    }
}
